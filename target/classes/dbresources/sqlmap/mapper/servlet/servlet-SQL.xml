<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="servlet">

	<select id="sidonm" resultType="servletVo">
		SELECT DISTINCT sd_nm FROM public.total ORDER BY sd_nm
	</select>
	
	<select id="sgg" resultType="servletVo" parameterType="String">
		SELECT DISTINCT sgg_nm FROM public.total WHERE sd_nm=#{sd } ORDER BY sgg_nm
	</select>
</mapper>